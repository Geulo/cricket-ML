This directory holds **`synthetic_smoke.csv`** (generated by `scripts/gen_synthetic_ipl.py`).

That file exists **only** so `pytest` can run without downloading Kaggle data. It must **not** be used to claim real model accuracy—replace `data/IPL.csv` with the real IPL dataset for serious training and reporting.
